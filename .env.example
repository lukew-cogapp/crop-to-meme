# Base URL of the IIIF proxy in worker/, which is what makes Art Institute of
# Chicago images load (artic.edu 403s our origins). Leave unset and the AIC tab
# labels itself "(not working)".
VITE_AIC_IIIF_PROXY=https://crop-to-meme-iiif.<subdomain>.workers.dev
